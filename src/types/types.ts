export type Apocalypse = {
  type: string;
  description: string;
  population: string;
  period: string;
};

export type BunkerData = {
  description: string;
  food: string;
  structure: string;
  additional: string;
};